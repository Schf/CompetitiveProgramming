# CP4 book 1, Exercise 2.2.2.2:

One possible solution is to sum the largest absolute value among the negative
numbers to all numbers on the array, sort the array, and subtract the added
value afterwards. This can be trivially done in O(N), which doesn't change the
algorithm's time complexity.
